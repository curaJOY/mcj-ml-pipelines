{
  "db": "postgresql",
  "db_settings": {
    "user": "${PGUSER}",
    "password": "${PGPASSWORD}",
    "host": "${PGHOST}",
    "port": "${PGPORT}",
    "dbname": "${PGDATABASE}",
    "sslmode": "require"
  }
}
